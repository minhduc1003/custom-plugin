import { Plugin } from 'vite';
import { PluginConfiguration } from './interface';
/**
 * Inject the CSS compiled with JS.
 *
 * @return {Plugin}
 */
export default function cssInjectedByJsPlugin({ injectCode, injectCodeFunction, jsAssetsFilterFunction, preRenderCSSCode, styleId, topExecutionPriority, useStrictCSP, }?: PluginConfiguration | undefined): Plugin;
