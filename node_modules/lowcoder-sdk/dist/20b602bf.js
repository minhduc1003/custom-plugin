import { W as l, J as a, _ as n } from "./c988e26e.js";
import "react";
import "react-dom";
test("test params control", () => {
  const o = "xx{{1+2+x}}sd{{-1}}";
  let t = new l({
    value: o
  });
  t = a(t), expect(t.toJsonValue()).toEqual(o), expect(n.mapValues(t.getView(), (e) => e({
    x: 4
  }))).toEqual({
    "-1": -1,
    "1+2+x": 7
  });
  const c = "{{3+2}}";
  t = t.reduce(t.changeChildAction("text", "{{3+2}}")), t = a(t), expect(t.toJsonValue()).toEqual(c), expect(n.mapValues(t.getView(), (e) => e())).toEqual({
    "3+2": 5
  });
});
