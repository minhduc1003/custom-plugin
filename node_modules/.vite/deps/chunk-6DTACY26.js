import {
  sn
} from "./chunk-C4ZQG65I.js";

// node_modules/lowcoder-sdk/dist/951b5247.js
var a = typeof Element < "u";
var c = typeof Map == "function";
var i = typeof Set == "function";
var l = typeof ArrayBuffer == "function" && !!ArrayBuffer.isView;
function u(e, r) {
  if (e === r)
    return true;
  if (e && r && typeof e == "object" && typeof r == "object") {
    if (e.constructor !== r.constructor)
      return false;
    var f, t, n;
    if (Array.isArray(e)) {
      if (f = e.length, f != r.length)
        return false;
      for (t = f; t-- !== 0; )
        if (!u(e[t], r[t]))
          return false;
      return true;
    }
    var o;
    if (c && e instanceof Map && r instanceof Map) {
      if (e.size !== r.size)
        return false;
      for (o = e.entries(); !(t = o.next()).done; )
        if (!r.has(t.value[0]))
          return false;
      for (o = e.entries(); !(t = o.next()).done; )
        if (!u(t.value[1], r.get(t.value[0])))
          return false;
      return true;
    }
    if (i && e instanceof Set && r instanceof Set) {
      if (e.size !== r.size)
        return false;
      for (o = e.entries(); !(t = o.next()).done; )
        if (!r.has(t.value[0]))
          return false;
      return true;
    }
    if (l && ArrayBuffer.isView(e) && ArrayBuffer.isView(r)) {
      if (f = e.length, f != r.length)
        return false;
      for (t = f; t-- !== 0; )
        if (e[t] !== r[t])
          return false;
      return true;
    }
    if (e.constructor === RegExp)
      return e.source === r.source && e.flags === r.flags;
    if (e.valueOf !== Object.prototype.valueOf && typeof e.valueOf == "function" && typeof r.valueOf == "function")
      return e.valueOf() === r.valueOf();
    if (e.toString !== Object.prototype.toString && typeof e.toString == "function" && typeof r.toString == "function")
      return e.toString() === r.toString();
    if (n = Object.keys(e), f = n.length, f !== Object.keys(r).length)
      return false;
    for (t = f; t-- !== 0; )
      if (!Object.prototype.hasOwnProperty.call(r, n[t]))
        return false;
    if (a && e instanceof Element)
      return false;
    for (t = f; t-- !== 0; )
      if (!((n[t] === "_owner" || n[t] === "__v" || n[t] === "__o") && e.$$typeof) && !u(e[n[t]], r[n[t]]))
        return false;
    return true;
  }
  return e !== e && r !== r;
}
var p = function(r, f) {
  try {
    return u(r, f);
  } catch (t) {
    if ((t.message || "").match(/stack|recursion/i))
      return console.warn("react-fast-compare cannot handle circular refs"), false;
    throw t;
  }
};
var v = sn(p);

export {
  p,
  v
};
//# sourceMappingURL=chunk-6DTACY26.js.map
